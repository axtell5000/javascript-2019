const myAge = 33;
let message;

message = myAge >= 18 ? 'You can vote' : 'You cannot vote'
console.log(message);

const team = ['Candy', 'Shadow', 'Shadrack', 'John', 'Ian'];
const message2 = team.length <= 4 ? `Your team: ${team.length}` : 'Too many people in your team.';
console.log(message2);
